process {
    withName: SRATOOLS_PREFETCH {
        publishDir = [
            path: { "${params.outdir}/sra" },
            enabled: false
        ]

        ext.args = params.download_method == 'sratools' ? "--max-size ${params.sratools_max_size}" : ''
    }
}