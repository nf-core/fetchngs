nextflow_process {

    name "Test Process SRA_MERGE_SAMPLESHEET"
    script "modules/local/sra_merge_samplesheet.nf"
    process "SRA_MERGE_SAMPLESHEET"
    tag "sra_merge_samplesheet"
    tag "modules_local"

    test("Should run without failures") {

        when {
            params {
                outdir   = "$outputDir"
            }
            process {
                """
                input[0] = file("$baseDir/modules/local/tests/input/sra_merge_samplesheet/samplesheets/*", checkIfExists: true)
                input[1] = file("$baseDir/modules/local/tests/input/sra_merge_samplesheet/mappings/*", checkIfExists: true)
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() },
            )
        }

    }

}
