nextflow_pipeline {

    name "Test pipeline"
    script "../main.nf"
    config "../conf/test.config"
    tag "pipeline"
    tag "pipeline_fetchngs"

    test("Run with profile test") {

        when {
            params {
                outdir = "$outputDir"
            }
        }

        then {
            assert workflow.success
        }
    }
}
