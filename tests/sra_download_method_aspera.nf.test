nextflow_workflow {

    name "Test workflow: ../workflows/fetchngs.nf"
    script "../main.nf"
    workflow "FETCHNGS"
    tag "SRA_DOWNLOAD_METHOD_ASPERA"

    // Dependencies
    tag "SRA_IDS_TO_RUNINFO"
    tag "SRA_RUNINFO_TO_FTP"
    tag "ASPERA_CLI"
    tag "SRA_TO_SAMPLESHEET"
    tag "MULTIQC_MAPPINGS_CONFIG"

    test("Parameters: --download_method aspera") {

        when {
            params {
                outdir = "$outputDir"
                download_method = 'aspera'
            }
            workflow {
                """
                input[0] = Channel.from("DRX026011", "ERX1234253", "SRX6725035")
                """
            }
        }

        then {
            assertAll(
                { assert workflow.success},
                { assert snapshot(
                    path(workflow.out.samplesheet[0].toString()).fileName.toString(),
                    workflow.out.mappings,
                    workflow.out.sample_mappings,
                    workflow.out.sra_metadata,
                    //workflow.out.versions,
                    path("${outputDir}/pipeline_info/nf_core_fetchngs_software_versions.yml").fileName.toString()
                ).match() }
            )
        }
    }
}
